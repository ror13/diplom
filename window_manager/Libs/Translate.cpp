// This is generated file DO NOT EDIT
// run gettext.py instead

#include "stdafx.h"

#pragma setlocale("russian")

#include "Translate.h"
#include <assert.h>

LPCTSTR _TRANS(LPCTSTR str, int context)
{
#ifdef LANG_INTERFACE_RUS
	if (context == 0 && CString(_T("From")) == str)
	{
		return _T("C");
	}
	if (context == 0 && CString(_T("Image files (")) == str)
	{
		return _T("Файлы изображения (");
	}
	if (context == 0 && CString(_T("Restart computer")) == str)
	{
		return _T("Перезапуск компьютера");
	}
	if (context == 0 && CString(_T("Error: Server is too old")) == str)
	{
		return _T("Ошибка: сервер слишком старый");
	}
	if (context == 0 && CString(_T("Copy file %s to %s")) == str)
	{
		return _T("Копирование файла %s в %s");
	}
	if (context == 0 && CString(_T("Duration")) == str)
	{
		return _T("Длительность");
	}
	if (context == 0 && CString(_T("Select Schedule here")) == str)
	{
		return _T("Выберите расписание");
	}
	if (context == 0 && CString(_T("PPT View 2003")) == str)
	{
		return _T("PPT View 2003");
	}
	if (context == 0 && CString(_T("Update Site on %s player?")) == str)
	{
		return _T("Обновить сайт %s плеера?");
	}
	if (context == 0 && CString(_T("Enabled")) == str)
	{
		return _T("Разрешён");
	}
	if (context == 0 && CString(_T("Creeping line files (*.clf)|*.clf|Audio files (*.mp3)|*.mp3|All Files (*.*)|*.*||")) == str)
	{
		return _T("Файлы бегущей строки (*.clf)|*.clf|Аудио файлы (*.mp3)|*.mp3|All Files (*.*)|*.*||");
	}
	if (context == 0 && CString(_T("Station Mngr (%s)")) == str)
	{
		return _T("Мнджр cтанций (%s)");
	}
	if (context == 0 && CString(_T("WARNING!\n\nThis RESET function will ERASE ALL DATA AND SETTINGS\nstored on this device and cannot be undone.")) == str)
	{
		return _T("ВНИМАНИЕ!\n\nЭта функция инициализации УДАЛИТ ВСЕ ДАННЫЕ И НАСТРОЙКИ,\nнаходящиеся на данном устройстве без возможности восстановления.");
	}
	if (context == 0 && CString(_T("[not supported]")) == str)
	{
		return _T("[не поддерживается]");
	}
	if (context == 0 && CString(_T("Bad schedule file format!")) == str)
	{
		return _T("Неправильный формат файла расписания!");
	}
	if (context == 0 && CString(_T("Error save shedule file: %s")) == str)
	{
		return _T("Ошибка сохранения файла расписания: %s");
	}
	if (context == 0 && CString(_T("Bad connection type")) == str)
	{
		return _T("Неправильный тип соединения");
	}
	if (context == 0 && CString(_T("Bad password or user name")) == str)
	{
		return _T("Неправильный пароль или имя пользователя");
	}
	if (context == 0 && CString(_T("Copy schedule template media files")) == str)
	{
		return _T("Скопировать файлы шаблона расписания");
	}
	if (context == 0 && CString(_T("There are no any connected clients to stop schedule \"%s\"")) == str)
	{
		return _T("Нет подключенных клиентов для остановки расписания \"%s\"");
	}
	if (context == 0 && CString(_T("Stations")) == str)
	{
		return _T("Станции");
	}
	if (context == 0 && CString(_T("You have no rights to manage this schedule")) == str)
	{
		return _T("У Вас нет прав для управления этим расписанием");
	}
	if (context == 0 && CString(_T("Yes")) == str)
	{
		return _T("Да");
	}
	if (context == 0 && CString(_T(" hours, ")) == str)
	{
		return _T(" часов");
	}
	if (context == 0 && CString(_T("Task RSS")) == str)
	{
		return _T("Задание RSS");
	}
	if (context == 0 && CString(_T("Task PPT")) == str)
	{
		return _T("Задание PPT");
	}
	if (context == 0 && CString(_T("Enter document path here")) == str)
	{
		return _T(" ");
	}
	if (context == 0 && CString(_T("Invalid data format (must be: XX XX XX ...)")) == str)
	{
		return _T("Неверный формат данных (должно быть: XX XX XX ...)");
	}
	if (context == 0 && CString(_T("February")) == str)
	{
		return _T("Февраль");
	}
	if (context == 0 && CString(_T("More <<")) == str)
	{
		return _T("Больше <<");
	}
	if (context == 0 && CString(_T(" hour, ")) == str)
	{
		return _T(" час, ");
	}
	if (context == 0 && CString(_T("Schedule:")) == str)
	{
		return _T("Расписание:");
	}
	if (context == 0 && CString(_T("Task %s failed to save!\n%s\n")) == str)
	{
		return _T("Не удалось сохранить задание %s!\n%s\n");
	}
	if (context == 0 && CString(_T("Enter Default ")) == str)
	{
		return _T("Введите Общую ");
	}
	if (context == 0 && CString(_T("Background")) == str)
	{
		return _T("Фоновое");
	}
	if (context == 0 && CString(_T("Copy Schedule %s")) == str)
	{
		return _T("Копировать расписание %s");
	}
	if (context == 0 && CString(_T("Start folder")) == str)
	{
		return _T("Начальная папка");
	}
	if (context == 0 && CString(_T("Config error: need select solid area")) == str)
	{
		return _T("Ошибка конфигурации: нужно выбирать целую область");
	}
	if (context == 0 && CString(_T("Security error. Trial time is expired. Please, register.")) == str)
	{
		return _T("Ошибка безопасности. Пробный период закончен. Пожалуйста, зарегистрируйтесь.");
	}
	if (context == 0 && CString(_T("Disabled")) == str)
	{
		return _T("Запрещённый");
	}
	if (context == 0 && CString(_T("[unknown]:")) == str)
	{
		return _T("[неизвестный]:");
	}
	if (context == 0 && CString(_T("Time")) == str)
	{
		return _T("Время");
	}
	if (context == 0 && CString(_T("Error copy media file to folder: %s")) == str)
	{
		return _T("Ошибка копирования медиа-файла в папку:  %s");
	}
	if (context == 0 && CString(_T("You've chosen wrong day! Please, choose another one!")) == str)
	{
		return _T("Вы выбрали неверный день! Пожалуйста, выберите другой!");
	}
	if (context == 0 && CString(_T("User doesn't have Administrator right")) == str)
	{
		return _T("У пользователя нет прав администратора");
	}
	if (context == 0 && CString(_T("Date & Time")) == str)
	{
		return _T("День & Время");
	}
	if (context == 0 && CString(_T("Flash files (*.swf)|*.swf|All Files (*.*)|*.*||")) == str)
	{
		return _T("Флеш файлы (*.swf)|*.swf|Все файлы (*.*)|*.*||");
	}
	if (context == 0 && CString(_T("Years")) == str)
	{
		return _T("Годы");
	}
	if (context == 0 && CString(_T("View %d")) == str)
	{
		return _T("Окно просмотра %d");
	}
	if (context == 0 && CString(_T("Client with current PlayerName already connected")) == str)
	{
		return _T("Клиент с данным именем уже подсоединён");
	}
	if (context == 0 && CString(_T("deSelectObject error")) == str)
	{
		return _T("Оибка deSelectObject");
	}
	if (context == 0 && CString(_T("\nThe task will be removed from schedules:\n")) == str)
	{
		return _T("\nЗадание будет удалено из расписаний:\n");
	}
	if (context == 0 && CString(_T("Status")) == str)
	{
		return _T("Статус");
	}
	if (context == 0 && CString(_T("Monitors for view not found")) == str)
	{
		return _T("Мониторы для окна просмотра не найдены");
	}
	if (context == 0 && CString(_T("256 color")) == str)
	{
		return _T("256 цвет");
	}
	if (context == 0 && CString(_T("CreateDIBitmap error")) == str)
	{
		return _T("Ошибка CreateDIBitmap");
	}
	if (context == 0 && CString(_T("PDF files (*.pdf)|*.pdf|All Files (*.*)|*.*||")) == str)
	{
		return _T("PDF файлы (*.pdf)|*.pdf|Все Файлы (*.*)|*.*||");
	}
	if (context == 0 && CString(_T("June")) == str)
	{
		return _T("Июнь");
	}
	if (context == 0 && CString(_T("CreateCompatibleBitmap error")) == str)
	{
		return _T("Ошибка CreateCompatibleBitmap");
	}
	if (context == 0 && CString(_T("Enter Internet URL here")) == str)
	{
		return _T(" ");
	}
	if (context == 0 && CString(_T("Task %04d")) == str)
	{
		return _T("Задание %04d");
	}
	if (context == 0 && CString(_T("PID")) == str)
	{
		return _T("PID");
	}
	if (context == 0 && CString(_T("Comment")) == str)
	{
		return _T("Коментарий к заданию");
	}
	if (context == 0 && CString(_T("Copy schedule media files")) == str)
	{
		return _T("Скопировать медиа файлы расписания");
	}
	if (context == 0 && CString(_T("May")) == str)
	{
		return _T("Май");
	}
	if (context == 0 && CString(_T("None")) == str)
	{
		return _T("Ничего");
	}
	if (context == 0 && CString(_T(" Scheduler Message")) == str)
	{
		return _T(" Сообщение планировщика");
	}
	if (context == 0 && CString(_T("FALSE")) == str)
	{
		return _T("НЕТ");
	}
	if (context == 0 && CString(_T("Select action type here")) == str)
	{
		return _T(" ");
	}
	if (context == 0 && CString(_T("Enter message text here")) == str)
	{
		return _T(" ");
	}
	if (context == 0 && CString(_T("Schedule %d")) == str)
	{
		return _T("Расписание %d");
	}
	if (context == 0 && CString(_T("Unknown exception!!! (last pos - %s) %s, %d")) == str)
	{
		return _T("Неизвестное исключение!!! (последняя поз - %s) %s, %d");
	}
	if (context == 0 && CString(_T("Can't exist all schedule media")) == str)
	{
		return _T("Не хватает файлов для расписания");
	}
	if (context == 0 && CString(_T("Flash")) == str)
	{
		return _T("Флеш");
	}
	if (context == 0 && CString(_T("Are you sure you want to exit?")) == str)
	{
		return _T("Вы действительно хотите выйти?");
	}
	if (context == 0 && CString(_T("Can't open update info file %s")) == str)
	{
		return _T("Невозможно открыть файл описания обновления %s");
	}
	if (context == 0 && CString(_T("Stopped")) == str)
	{
		return _T("Остановлено");
	}
	if (context == 0 && CString(_T("Action type:")) == str)
	{
		return _T("Тип действия:");
	}
	if (context == 0 && CString(_T("There are no any connected clients to run schedule \"%s\"")) == str)
	{
		return _T("Нет подключенных клиентов для запуска расписания \"%s\"");
	}
	if (context == 0 && CString(_T("ViewID")) == str)
	{
		return _T("ID окна просмотра");
	}
	if (context == 0 && CString(_T("View %d (use)")) == str)
	{
		return _T("Окно просмотра %d (использовать)");
	}
	if (context == 0 && CString(_T("UPDATE files (")) == str)
	{
		return _T("Файлы ОБНОВЛЕНИЯ (");
	}
	if (context == 0 && CString(_T("Schedule name")) == str)
	{
		return _T("Имя расписания");
	}
	if (context == 0 && CString(_T("Logo file doesn't exist!")) == str)
	{
		return _T("Файл логотипа не существует!");
	}
	if (context == 0 && CString(_T("4 color")) == str)
	{
		return _T("4 цвета");
	}
	if (context == 0 && CString(_T("Enter Internet RSS here")) == str)
	{
		return _T(" ");
	}
	if (context == 0 && CString(_T("Update")) == str)
	{
		return _T("Обновление");
	}
	if (context == 0 && CString(_T("BitBlt error")) == str)
	{
		return _T("Ошибка BitBlt");
	}
	if (context == 0 && CString(_T("CMyException: %s: lasterror=%d (line %d, file %s)")) == str)
	{
		return _T("CMyException: %s: ошибка=%d (строка %d, файл %s)");
	}
	if (context == 0 && CString(_T("Turn monitor On")) == str)
	{
		return _T("Включение монитора");
	}
	if (context == 0 && CString(_T("March")) == str)
	{
		return _T("Март");
	}
	if (context == 0 && CString(_T("Task BT")) == str)
	{
		return _T("BT задание");
	}
	if (context == 0 && CString(_T("PowerPoint files (")) == str)
	{
		return _T("PowerPoint Файлы (");
	}
	if (context == 0 && CString(_T("Background file path")) == str)
	{
		return _T("Путь к файлу Фона");
	}
	if (context == 0 && CString(_T(" Manager")) == str)
	{
		return _T("Менеджер");
	}
	if (context == 0 && CString(_T("Running: ")) == str)
	{
		return _T("Работает: ");
	}
	if (context == 0 && CString(_T("DDERR_CANTLOCKSURFACE: Access to this surface is being refused because no driver exists which can supply a pointer to the surface.")) == str)
	{
		return _T("DDERR_CANTLOCKSURFACE: Доступ к этой поверхности был отклонён из-за отсутствия драйвера, который может вернуть указатель на поверхность.");
	}
	if (context == 0 && CString(_T("CDirectDrawInfo ERROR: %s (line %d, file %s)")) == str)
	{
		return _T("ОШИБКА CDirectDrawInfo: %s (строка %d, файл %s)");
	}
	if (context == 0 && CString(_T("You have no rights to manage all Clients in this schedule.\nClient filter will be reduced to your Managed Clients")) == str)
	{
		return _T("У вас нет прав для управления всеми Клиентами в этом расписании.\nФильтр клиентов будет сокращён до управляемых вами Клиентов");
	}
	if (context == 0 && CString(_T("%d bits")) == str)
	{
		return _T("%d бит");
	}
	if (context == 0 && CString(_T("Data can't be empty")) == str)
	{
		return _T("Поле не может быть пустым");
	}
	if (context == 0 && CString(_T("hours")) == str)
	{
		return _T("часов");
	}
	if (context == 0 && CString(_T("Are you sure to shutdown %s player?")) == str)
	{
		return _T("Выполнить выключение плеера %s?");
	}
	if (context == 0 && CString(_T("P&review")) == str)
	{
		return _T("П&росмотр");
	}
	if (context == 0 && CString(_T("October")) == str)
	{
		return _T("Октябрь");
	}
	if (context == 0 && CString(_T("Even")) == str)
	{
		return _T("Чётный");
	}
	if (context == 0 && CString(_T("PowerPoint files (*.ppt;*.pptx;*.pptm;*.pps;*.ppsx;*.pot;*.potx;*.potm)|*.ppt;*.pptx;*.pptm;*.pps;*.ppsx;*.pot;*.potx;*.potm|All Files (*.*)|*.*||")) == str)
	{
		return _T("Файлы презентации (*.ppt;*.pptx;*.pptm;*.pps;*.ppsx;*.pot;*.potx;*.potm)|*.ppt;*.pptx;*.pptm;*.pps;*.ppsx;*.pot;*.potx;*.potm|Все файлы (*.*)|*.*||");
	}
	if (context == 0 && CString(_T("Super")) == str)
	{
		return _T("Супер");
	}
	if (context == 0 && CString(_T("Month")) == str)
	{
		return _T("Месяц");
	}
	if (context == 0 && CString(_T("Invalid monitor configuration:\nWall view #%d has invalid geometry and will be deleted.")) == str)
	{
		return _T("Неверная конфигурация мониторов:\nУ окон просмотра видеостены #%d неверная геометрия и он будет удалён.");
	}
	if (context == 0 && CString(_T("Do you want to delete task?")) == str)
	{
		return _T("Вы действительно хотите удалить это задание?");
	}
	if (context == 0 && CString(_T("URL")) == str)
	{
		return _T("URL");
	}
	if (context == 0 && CString(_T("Unknown error: %X (%d)")) == str)
	{
		return _T("Неизвестная ошибка: %X (%d)");
	}
	if (context == 0 && CString(_T("Video files (.asf;.avi;.divx;.dv;.flv;.m1v;.m2v;.mkv;.mov;.mp4;.mpeg;.mpeg1;.mpeg2;.mpeg4;.mpg;.ps;.ts;.ogm;.vob;.wmv)|*.asf;*.avi;*.divx;*.dv;*.flv;*.m1v;*.m2v;*.mkv;*.mov;*.mp4;*.mpeg;*.mpeg1;*.mpeg2;*.mpeg4;*.mpg;*.ps;*.ts;*.ogm;*.vob;*.wmv|All Files (*.*)|*.*||")) == str)
	{
		return _T("Видео файлы (.asf;.avi;.divx;.dv;.flv;.m1v;.m2v;.mkv;.mov;.mp4;.mpeg;.mpeg1;.mpeg2;.mpeg4;.mpg;.ps;.ts;.ogm;.vob;.wmv)|*.asf;*.avi;*.divx;*.dv;*.flv;*.m1v;*.m2v;*.mkv;*.mov;*.mp4;*.mpeg;*.mpeg1;*.mpeg2;*.mpeg4;*.mpg;*.ps;*.ts;*.ogm;*.vob;*.wmv|All Files (*.*)|*.*||");
	}
	if (context == 0 && CString(_T("RS232 Data")) == str)
	{
		return _T("Данные RS232");
	}
	if (context == 0 && CString(_T("Can't open file \"%s\"!")) == str)
	{
		return _T("Невозможно открыть файл \"%s\"!");
	}
	if (context == 0 && CString(_T("Video files (")) == str)
	{
		return _T("Video Файлы (");
	}
	if (context == 0 && CString(_T("PPT View 97")) == str)
	{
		return _T("PPT View 97");
	}
	if (context == 0 && CString(_T("Trigger (%s)")) == str)
	{
		return _T("Триггер (%s)");
	}
	if (context == 0 && CString(_T("file \'wall_views.ini\' not found")) == str)
	{
		return _T("файл \'wall_views.ini\' не найден");
	}
	if (context == 0 && CString(_T("Are you sure to identify %s player?")) == str)
	{
		return _T("Вы уверены что хотите идетнифицировать плеер %s?");
	}
	if (context == 0 && CString(_T("File path")) == str)
	{
		return _T("Путь к файлу");
	}
	if (context == 0 && CString(_T("Info")) == str)
	{
		return _T("Информация");
	}
	if (context == 0 && CString(_T("Not definite")) == str)
	{
		return _T("Не определен");
	}
	if (context == 0 && CString(_T("Set %d")) == str)
	{
		return _T("Сет %d");
	}
	if (context == 0 && CString(_T("not set")) == str)
	{
		return _T("не установлен");
	}
	if (context == 0 && CString(_T(" Client is not available!")) == str)
	{
		return _T("Клиент недоступен!");
	}
	if (context == 0 && CString(_T("Are you sure to shutdown %s group of player boxis?")) == str)
	{
		return _T("Выполнить выключение группы плееров %s?");
	}
	if (context == 0 && CString(_T("Wall Monitors configuration changed! It will be restore!")) == str)
	{
		return _T("Конфигурация мониторов видеостены изменилась! Она будет восстановлена!");
	}
	if (context == 0 && CString(_T("Limit used date to:\t%s")) == str)
	{
		return _T("Ограничить дату использования до:\t%s");
	}
	if (context == 0 && CString(_T("TRUE")) == str)
	{
		return _T("ДА");
	}
	if (context == 0 && CString(_T("Bottom")) == str)
	{
		return _T("Внизу");
	}
	if (context == 0 && CString(_T("Processes")) == str)
	{
		return _T("Процессы");
	}
	if (context == 0 && CString(_T("%.0f pixels / second")) == str)
	{
		return _T("%.0f пикселей / секунду");
	}
	if (context == 0 && CString(_T("Select port here")) == str)
	{
		return _T(" ");
	}
	if (context == 0 && CString(_T("December")) == str)
	{
		return _T("Декабрь");
	}
	if (context == 0 && CString(_T("unknown")) == str)
	{
		return _T("неизвестный");
	}
	if (context == 0 && CString(_T("Internet &RSS:")) == str)
	{
		return _T("Интернет &RSS:");
	}
	if (context == 0 && CString(_T("Can't save massive of tasks!\n")) == str)
	{
		return _T("Массив заданий не сохранён!\n");
	}
	if (context == 0 && CString(_T("Type")) == str)
	{
		return _T("Тип");
	}
	if (context == 0 && CString(_T("RS232 Output Port:")) == str)
	{
		return _T("Порт выхода RS232:");
	}
	if (context == 0 && CString(_T("Are you sure send Wake-On-Lan request to %s group of player boxis?")) == str)
	{
		return _T("Вы действительно хотите послать Wake-On-Lan запрос на %s группу плееров?");
	}
	if (context == 0 && CString(_T(" minutes, ")) == str)
	{
		return _T(" минут, ");
	}
	if (context == 0 && CString(_T("Command has no binary part")) == str)
	{
		return _T("У команды нет бинарной части");
	}
	if (context == 0 && CString(_T("Error! %s not found")) == str)
	{
		return _T("Ошибка! %s не найден");
	}
	if (context == 0 && CString(_T("UsePPTView")) == str)
	{
		return _T("UsePPTView");
	}
	if (context == 0 && CString(_T("November")) == str)
	{
		return _T("Ноябрь");
	}
	if (context == 0 && CString(_T("Can't exist all task media")) == str)
	{
		return _T("Не хватает файлов для задач");
	}
	if (context == 0 && CString(_T("%s Version %s")) == str)
	{
		return _T("%s Версия %s");
	}
	if (context == 0 && CString(_T("%s (downloading media %s of %s)")) == str)
	{
		return _T("%s (загружаются медиа-файлы %s of %s)");
	}
	if (context == 0 && CString(_T("Task PDF")) == str)
	{
		return _T("Задание PDF");
	}
	if (context == 0 && CString(_T("All Screen")) == str)
	{
		return _T("Весь Экран");
	}
	if (context == 0 && CString(_T("New Task")) == str)
	{
		return _T("Новое Задание");
	}
	if (context == 0 && CString(_T("User Manager")) == str)
	{
		return _T("Менеджер Пользователей");
	}
	if (context == 0 && CString(_T("Change %s display properties")) == str)
	{
		return _T("Изменить свойства вывода %s");
	}
	if (context == 0 && CString(_T("July")) == str)
	{
		return _T("Июль");
	}
	if (context == 0 && CString(_T("Task URL")) == str)
	{
		return _T("URL задание");
	}
	if (context == 0 && CString(_T("Turn monitor Off")) == str)
	{
		return _T("Выключение монитора");
	}
	if (context == 0 && CString(_T("Task Unknown")) == str)
	{
		return _T("Неизвестное задание");
	}
	if (context == 0 && CString(_T("Times Played")) == str)
	{
		return _T("Раз проиграно");
	}
	if (context == 0 && CString(_T("Run on player start")) == str)
	{
		return _T("Запуск по включении");
	}
	if (context == 0 && CString(_T("Before try!")) == str)
	{
		return _T("Перед попыткой!");
	}
	if (context == 0 && CString(_T("Can't create Schedule file: %s")) == str)
	{
		return _T("Невозможно создать файл Расписания: %s");
	}
	if (context == 0 && CString(_T("Can't calc MD5 for file: %s")) == str)
	{
		return _T("Невозможно посчитать MD5 для файла: %s");
	}
	if (context == 0 && CString(_T(" minute, ")) == str)
	{
		return _T(" минута, ");
	}
	if (context == 0 && CString(_T("Run once")) == str)
	{
		return _T("Запуск один раз");
	}
	if (context == 0 && CString(_T("Filtered")) == str)
	{
		return _T("Отфильтровано");
	}
	if (context == 0 && CString(_T("Group name")) == str)
	{
		return _T("Имя Группы");
	}
	if (context == 0 && CString(_T("Client")) == str)
	{
		return _T("Клиент");
	}
	if (context == 0 && CString(_T(" Client")) == str)
	{
		return _T(" Клиент");
	}
	if (context == 0 && CString(_T("Trigger")) == str)
	{
		return _T("Триггер");
	}
	if (context == 0 && CString(_T("Template files (.htm;.html)|*.htm;*.html|All Files (*.*)|*.*||")) == str)
	{
		return _T("Файлы шаблонов (.htm;.html)|*.htm;*.html|All Files (*.*)|*.*||");
	}
	if (context == 0 && CString(_T("Failed to delete group")) == str)
	{
		return _T("Ошибка удаления группы");
	}
	if (context == 0 && CString(_T("Stream Video")) == str)
	{
		return _T("Потоковое видео");
	}
	if (context == 0 && CString(_T("View%d")) == str)
	{
		return _T("Окно просмотра%d");
	}
	if (context == 0 && CString(_T("Are you sure to restart %s group of player boxis?")) == str)
	{
		return _T("Выполнить перезагрузку группы плееров %s?");
	}
	if (context == 0 && CString(_T("FlashQuality")) == str)
	{
		return _T("FlashQuality");
	}
	if (context == 0 && CString(_T("High Flash quality")) == str)
	{
		return _T("Флеш высокого качества");
	}
	if (context == 0 && CString(_T("File Name")) == str)
	{
		return _T("Имя Файла");
	}
	if (context == 0 && CString(_T("Text:")) == str)
	{
		return _T("Текст:");
	}
	if (context == 0 && CString(_T(" Station Manager")) == str)
	{
		return _T(" Менеджер станций");
	}
	if (context == 0 && CString(_T("Tasks")) == str)
	{
		return _T("Задания");
	}
	if (context == 0 && CString(_T("Top")) == str)
	{
		return _T("Вверху");
	}
	if (context == 0 && CString(_T("Task Video")) == str)
	{
		return _T("Задание Видео");
	}
	if (context == 0 && CString(_T("The task used in schedules:\n")) == str)
	{
		return _T("Задание используется в расписаниях:\n");
	}
	if (context == 0 && CString(_T("Manager's licenses: %d\tUsed: %d\n")) == str)
	{
		return _T("Лицензии Менеджера: %d\tИспользовано: %d\n");
	}
	if (context == 0 && CString(_T("Server works in evaluation mode! %d days left.\nPlease, register. Sorry for any inconviniences.")) == str)
	{
		return _T("Сервер работает в пробном режиме! Осталось %d дней.\nПожалуйста, зарегистрируйтесь. Приносим извинения за неудобства.");
	}
	if (context == 0 && CString(_T("Task Flash")) == str)
	{
		return _T("Задание Флеш");
	}
	if (context == 0 && CString(_T("Are you sure to change the template?")) == str)
	{
		return _T("Вы действительно хотите поменять шаблон?");
	}
	if (context == 0 && CString(_T("View ID")) == str)
	{
		return _T("ID окна просмотра");
	}
	if (context == 0 && CString(_T("Unable to get report for %s days, player is not connected")) == str)
	{
		return _T("Нет возможности получить отчёт на %s дни, плеер не подключен.");
	}
	if (context == 0 && CString(_T("Auto restart")) == str)
	{
		return _T("Авто перезапуск");
	}
	if (context == 0 && CString(_T(" days, ")) == str)
	{
		return _T(" дней, ");
	}
	if (context == 0 && CString(_T("Default")) == str)
	{
		return _T("Общая");
	}
	if (context == 0 && CString(_T("View name")) == str)
	{
		return _T("Имя окна просмотра");
	}
	if (context == 0 && CString(_T("CreateCompatibleDC 2 error")) == str)
	{
		return _T("Ошибка CreateCompatibleDC 2");
	}
	if (context == 0 && CString(_T("User")) == str)
	{
		return _T("Пользователь");
	}
	if (context == 0 && CString(_T("April")) == str)
	{
		return _T("Апрель");
	}
	if (context == 0 && CString(_T("2 color")) == str)
	{
		return _T("2 цвет");
	}
	if (context == 0 && CString(_T("Unknown")) == str)
	{
		return _T("Неизвестный");
	}
	if (context == 0 && CString(_T("File %s exist! Overwrite?")) == str)
	{
		return _T("Файл %s существует! Перезаписать?");
	}
	if (context == 0 && CString(_T("Odd")) == str)
	{
		return _T("Нечётный");
	}
	if (context == 0 && CString(_T("GetDC error")) == str)
	{
		return _T("Ошибка GetDC");
	}
	if (context == 0 && CString(_T("deSelectObject 2 error")) == str)
	{
		return _T("Ошибка deSelectObject 2");
	}
	if (context == 0 && CString(_T("Not enought memory to get file list. To many selected files.")) == str)
	{
		return _T("Много выделенных файлов. Нехватает памяти.");
	}
	if (context == 0 && CString(_T("Unknown (%s)")) == str)
	{
		return _T("Неизвестный (%s)");
	}
	if (context == 0 && CString(_T("January")) == str)
	{
		return _T("Январь");
	}
	if (context == 0 && CString(_T("File Size")) == str)
	{
		return _T("Размер файла");
	}
	if (context == 0 && CString(_T("Excel files (")) == str)
	{
		return _T("Excel Файлы (");
	}
	if (context == 0 && CString(_T("Video Input")) == str)
	{
		return _T("Видео вход");
	}
	if (context == 0 && CString(_T("Task Manager")) == str)
	{
		return _T("Менеджер заданий");
	}
	if (context == 0 && CString(_T("Internet &URL:")) == str)
	{
		return _T("Интернет &URL:");
	}
	if (context == 0 && CString(_T("Whole Duration")) == str)
	{
		return _T("Полная длительность");
	}
	if (context == 0 && CString(_T("File %s doesn't exist! Not all media found!")) == str)
	{
		return _T("Файл %s не существует! Не все медиа-файлы найдены!");
	}
	if (context == 0 && CString(_T("Write Failed!")) == str)
	{
		return _T("Ошибка записи!");
	}
	if (context == 0 && CString(_T("Site")) == str)
	{
		return _T("Сайт");
	}
	if (context == 0 && CString(_T("CreateCompatibleDC error")) == str)
	{
		return _T("Ошибка CreateCompatibleDC");
	}
	if (context == 0 && CString(_T("PDF &file:")) == str)
	{
		return _T("PDF &файл:");
	}
	if (context == 0 && CString(_T("Mouse and keyboard are redirected into %s!\nPress %s to continue working with current station.\n")) == str)
	{
		return _T("Мышь и клавиатура перенаправлены в %s!\nНажмите %s для продолжения работы с данной станцией.\n");
	}
	if (context == 0 && CString(_T("Error download file:\n%s")) == str)
	{
		return _T("Ошибка скачивания файла:\n%s");
	}
	if (context == 0 && CString(_T("\nBut you have no rights to manage all schedules' clients")) == str)
	{
		return _T("\nНо у Вас нет прав для управления всеми клиентами расписаний");
	}
	if (context == 0 && CString(_T("Station")) == str)
	{
		return _T("Станция");
	}
	if (context == 0 && CString(_T("ALL")) == str)
	{
		return _T("ВСЕ");
	}
	if (context == 0 && CString(_T("Viewset")) == str)
	{
		return _T("Набор окон просмотра");
	}
	if (context == 0 && CString(_T("Failed to set new name for the group")) == str)
	{
		return _T("Ошибка переименования");
	}
	if (context == 0 && CString(_T("Enter file path here")) == str)
	{
		return _T(" ");
	}
	if (context == 0 && CString(_T("\nPress Ctrl+Shift+%d to switch to %s")) == str)
	{
		return _T("\nНажмите Ctrl+Shift+%d для перехода к %s");
	}
	if (context == 0 && CString(_T("Image &file:")) == str)
	{
		return _T("&Файл изображения:");
	}
	if (context == 0 && CString(_T("NEVER!")) == str)
	{
		return _T("НИКОГДА!");
	}
	if (context == 0 && CString(_T("Task type: %s\nMedia file: %s")) == str)
	{
		return _T("Тип задания: %s\nФайл: %s");
	}
	if (context == 0 && CString(_T("NONE!")) == str)
	{
		return _T("НИЧЕГО!");
	}
	if (context == 0 && CString(_T("Can't save report to file: %s")) == str)
	{
		return _T("Невозможно сохранить отчёт в файл: %s");
	}
	if (context == 0 && CString(_T("Hide")) == str)
	{
		return _T("Спрятать");
	}
	if (context == 0 && CString(_T("Limit used number: %d\n")) == str)
	{
		return _T("Ограничить used number: %d\n");
	}
	if (context == 0 && CString(_T("Video Input Device:")) == str)
	{
		return _T("Выбрать устройство:");
	}
	if (context == 0 && CString(_T("::Presenter Client")) == str)
	{
		return _T("::Presenter клиент");
	}
	if (context == 0 && CString(_T("MD5")) == str)
	{
		return _T("MD5");
	}
	if (context == 0 && CString(_T("Client (%s)")) == str)
	{
		return _T("Клиент (%s)");
	}
	if (context == 0 && CString(_T("Update Site on %s group of players?")) == str)
	{
		return _T("Обновить сайт %s группе плееров");
	}
	if (context == 0 && CString(_T("SelectObject error")) == str)
	{
		return _T("ошибка SelectObject");
	}
	if (context == 0 && CString(_T("More >>")) == str)
	{
		return _T("Больше >>");
	}
	if (context == 0 && CString(_T("Group")) == str)
	{
		return _T("Группа");
	}
	if (context == 0 && CString(_T("Installer time stamp: %s")) == str)
	{
		return _T("Дата инсталлятора: %s");
	}
	if (context == 0 && CString(_T("Edit only .clf files")) == str)
	{
		return _T("Редактировать только .clf файлы");
	}
	if (context == 0 && CString(_T("You can not change the name of default group items!")) == str)
	{
		return _T("Вы не можете изменять имена групп, созданных по умолчанию!");
	}
	if (context == 0 && CString(_T("Limit used date from:\t%s")) == str)
	{
		return _T("Ограничить использованную дату с:\t%s");
	}
	if (context == 0 && CString(_T("[unknown]")) == str)
	{
		return _T("[неизвестен]");
	}
	if (context == 0 && CString(_T("PDF files (")) == str)
	{
		return _T("PDF Файлы (");
	}
	if (context == 0 && CString(_T("August")) == str)
	{
		return _T("Август");
	}
	if (context == 0 && CString(_T("SelectObject 2 error")) == str)
	{
		return _T("ошибка SelectObject 2");
	}
	if (context == 0 && CString(_T("PDF")) == str)
	{
		return _T("PDF");
	}
	if (context == 0 && CString(_T("All Files (*.*)|*.*||")) == str)
	{
		return _T("Все файлы (*.*)|*.*||");
	}
	if (context == 0 && CString(_T("Period")) == str)
	{
		return _T("Период");
	}
	if (context == 0 && CString(_T("Password and Confirm password mismacth!\n")) == str)
	{
		return _T("Введенные пароли не совпадают!\n");
	}
	if (context == 0 && CString(_T("Move schedule to Group")) == str)
	{
		return _T("Переместить расписание в Группу");
	}
	if (context == 0 && CString(_T("Excel &file:")) == str)
	{
		return _T("Excel &файл:");
	}
	if (context == 0 && CString(_T("Are you sure to restart %s player?")) == str)
	{
		return _T("Выполнить перезагрузку плеера %s?");
	}
	if (context == 0 && CString(_T("Interval")) == str)
	{
		return _T("Интервал");
	}
	if (context == 0 && CString(_T("Enter Internet URI here")) == str)
	{
		return _T("Введите адрес URI");
	}
	if (context == 0 && CString(_T("Encryption error!")) == str)
	{
		return _T("Ошибка шифрования!");
	}
	if (context == 0 && CString(_T("Failed to set group properties")) == str)
	{
		return _T("Ошибка установки свойств группы");
	}
	if (context == 0 && CString(_T("Do you want to delete user?")) == str)
	{
		return _T("Вы действительно хотите удалить пользователя?");
	}
	if (context == 0 && CString(_T("16 color")) == str)
	{
		return _T("16 цвет");
	}
	if (context == 0 && CString(_T(" User Manager")) == str)
	{
		return _T(" Менеджер пользователей");
	}
	if (context == 0 && CString(_T("New Schedule")) == str)
	{
		return _T("Новое Расписание");
	}
	if (context == 0 && CString(_T("Now sending %s of %s (left %d file(s))")) == str)
	{
		return _T("Сейчас посылается %s из %s (осталось %d файл(ов))");
	}
	if (context == 0 && CString(_T("Station Manager")) == str)
	{
		return _T("Менеджер станций");
	}
	if (context == 0 && CString(_T("Are you sure send Wake-On-Lan request to %s player box?")) == str)
	{
		return _T("Вы действительно хотите послать Wake-On-Lan запрос на %s плеер?");
	}
	if (context == 0 && CString(_T("Select device here")) == str)
	{
		return _T(" ");
	}
	if (context == 0 && CString(_T("&Message text:")) == str)
	{
		return _T("Текст &сообщения:");
	}
	if (context == 0 && CString(_T("Flash files (")) == str)
	{
		return _T("Flash Файлы (");
	}
	if (context == 0 && CString(_T("Security error. Can't find usb key device.")) == str)
	{
		return _T("Ошибка безопасности. Не найден usb-ключ.");
	}
	if (context == 0 && CString(_T(" %s rev: %s, msg %d")) == str)
	{
		return _T(" %s рев: %s, сообщ %d");
	}
	if (context == 0 && CString(_T("Limit runtime: %d days %d hours %d minutes\n")) == str)
	{
		return _T("Ограничить время работы: %d дней %d часов %d минут\n");
	}
	if (context == 0 && CString(_T("User doesn't have Manager right")) == str)
	{
		return _T("У пользователя нет прав менеджера");
	}
	if (context == 0 && CString(_T("Available")) == str)
	{
		return _T("Доступные");
	}
	if (context == 0 && CString(_T("Test text Creeping Line")) == str)
	{
		return _T("Пробный текст бегущей строки");
	}
	if (context == 0 && CString(_T("Excel Files (*.xls)|*.xls|All Files (*.*)|*.*||")) == str)
	{
		return _T("Excel Файлы (*.xls)|*.xls|Все файлы (*.*)|*.*||");
	}
	if (context == 0 && CString(_T("No")) == str)
	{
		return _T("Нет");
	}
	if (context == 0 && CString(_T("Week days")) == str)
	{
		return _T("Дни недели");
	}
	if (context == 0 && CString(_T("Internet &URI:")) == str)
	{
		return _T("Адрес &URI");
	}
	if (context == 0 && CString(_T("View set name")) == str)
	{
		return _T("Имя набора окон просмотра");
	}
	if (context == 0 && CString(_T("User Mngr (%s)")) == str)
	{
		return _T("Мнджр Пользователя (%s)");
	}
	if (context == 0 && CString(_T("Task Excel")) == str)
	{
		return _T("Задание Excel");
	}
	if (context == 0 && CString(_T("User name")) == str)
	{
		return _T("Имя пользователя");
	}
	if (context == 0 && CString(_T("Excel")) == str)
	{
		return _T("Excel");
	}
	if (context == 0 && CString(_T("Bad protocol version")) == str)
	{
		return _T("Неправильная версия протокола");
	}
	if (context == 0 && CString(_T("Process")) == str)
	{
		return _T("Процесс");
	}
	if (context == 0 && CString(_T("Unable read picture file!")) == str)
	{
		return _T("Невозможно прочесть файл изображения!");
	}
	if (context == 0 && CString(_T("Client ID")) == str)
	{
		return _T("ID клиента");
	}
	if (context == 0 && CString(_T("Task Image")) == str)
	{
		return _T("Задание Изображение");
	}
	if (context == 0 && CString(_T("Days of month")) == str)
	{
		return _T("Дни месяца");
	}
	if (context == 0 && CString(_T("September")) == str)
	{
		return _T("Сентябрь");
	}
	if (context == 0 && CString(_T("Days of week")) == str)
	{
		return _T("Дни недели");
	}
	if (context == 0 && CString(_T("Client is too old")) == str)
	{
		return _T("Клиент слишком старый");
	}
	if (context == 0 && CString(_T("Can't open file: %s")) == str)
	{
		return _T("Невозможно открыть файл: %s");
	}
	if (context == 0 && CString(_T("URL:")) == str)
	{
		return _T("URL:");
	}
	if (context == 0 && CString(_T("Copy Task %s files")) == str)
	{
		return _T("Копировать файлы задания %s");
	}
	if (context == 0 && CString(_T("Are you sure to identify %s group of player boxis?")) == str)
	{
		return _T("Вы уверены что хотите идетнифицировать группу плееров %s?");
	}
	if (context == 0 && CString(_T("Line: ")) == str)
	{
		return _T("Строка: ");
	}
	if (context == 0 && CString(_T("Image files (.bmp;.gif;.jpg;.jpeg;.png;.tif;.tiff)|*.bmp;*.gif;*.jpg;*.jpeg;*.png;*.tif;*.tiff|All Files (*.*)|*.*||")) == str)
	{
		return _T("Файлы изображения (.bmp;.gif;.jpg;.jpeg;.png;.tif;.tiff)|*.bmp;*.gif;*.jpg;*.jpeg;*.png;*.tif;*.tiff|Все Файлы (*.*)|*.*||");
	}
	if (context == 0 && CString(_T("UPDATE files (*.img)|*.img|All Files (*.*)|*.*||")) == str)
	{
		return _T("Файлы ОБНОВЛЕНИЯ (*.img)|*.img|Все Файлы (*.*)|*.*||");
	}
	if (context == 0 && CString(_T("You should fill \"%s\" field!\n")) == str)
	{
		return _T("Вы должны заполнить поле \"%s\"!\n");
	}
	if (context == 0 && CString(_T("View")) == str)
	{
		return _T("Окно просмотра");
	}
	if (context == 0 && CString(_T("Not enough memory to allocate %d bytes for temporary buffer!")) == str)
	{
		return _T("Недостаточно памяти для выделения %d байт для временного буфера!");
	}
	if (context == 0 && CString(_T("Invalid logo file format!")) == str)
	{
		return _T("Неверный формат файла логотипа");
	}
	if (context == 0 && CString(_T("User Name")) == str)
	{
		return _T("Имя пользователя");
	}
	if (context == 0 && CString(_T("Schedules")) == str)
	{
		return _T("Расписания");
	}
	if (context == 0 && CString(_T("AutoRestart")) == str)
	{
		return _T("Автоперезапуск");
	}
	if (context == 0 && CString(_T("Copy file \"%s\" to \"%s\"")) == str)
	{
		return _T("Копирование файла \"%s\" в \"%s\"");
	}
	if (context == 0 && CString(_T("Can't create template: %s")) == str)
	{
		return _T("Невозможно создать шаблон: %s");
	}
	if (context == 0 && CString(_T("%.1f pixels / second")) == str)
	{
		return _T("%.1f пикселей / секунду");
	}
	if (context == 0 && CString(_T("Name")) == str)
	{
		return _T("Имя");
	}
	if (context == 0 && CString(_T("Flash &document:")) == str)
	{
		return _T("Флеш &документ:");
	}
	if (context == 0 && CString(_T("Video &file:")) == str)
	{
		return _T("Видео &файл:");
	}
	if (context == 0 && CString(_T("Manager")) == str)
	{
		return _T("Менеджер");
	}
	if (context == 0 && CString(_T("\n\nRemote ")) == str)
	{
		return _T("\n\nУдалённый ");
	}
	if (context == 0 && CString(_T("Update &file:")) == str)
	{
		return _T("&Файл обновления:");
	}
	if (context == 0 && CString(_T("Attention")) == str)
	{
		return _T("Внимание");
	}
	if (context == 0 && CString(_T("Client Name")) == str)
	{
		return _T("Имя клиента");
	}
	if (context == 0 && CString(_T(" seconds")) == str)
	{
		return _T(" секунд");
	}
	if (context == 0 && CString(_T("Remote %s Administration")) == str)
	{
		return _T("Удалённое %s администрирование");
	}
	if (context == 0 && CString(_T("Background &document:")) == str)
	{
		return _T("Фоновый &документ:");
	}
	if (context == 0 && CString(_T("Hidden")) == str)
	{
		return _T("Скрытое");
	}
	if (context == 0 && CString(_T("minutes")) == str)
	{
		return _T("минут");
	}
	if (context == 0 && CString(_T("?? color")) == str)
	{
		return _T("?? цвет");
	}
	if (context == 0 && CString(_T("Months")) == str)
	{
		return _T("Месяцы");
	}
	if (context == 0 && CString(_T("PowerPoint &document:")) == str)
	{
		return _T("&Документ презентации:");
	}
	if (context == 0 && CString(_T("Vers: Protocol %s, Task %d, Schedule %d, Super Schedule %d")) == str)
	{
		return _T("Версии: Протокол %s, Задание %d, Расписание %d, Суперрасписание %d");
	}
	if (context == 0 && CString(_T("Invalid char in data (no hex)")) == str)
	{
		return _T("Неверный символ в данных (no hex)");
	}
	if (context == 0 && CString(_T("Window")) == str)
	{
		return _T("Окно");
	}
	if (context == 0 && CString(_T("Special Action")) == str)
	{
		return _T("Специальное действие");
	}
	if (context == 0 && CString(_T("Site name")) == str)
	{
		return _T("Название сайта");
	}
	if (context == 0 && CString(_T("Video")) == str)
	{
		return _T("Видео");
	}
	if (context == 0 && CString(_T("Delete all schedules and tasks from the group before its deleting, please")) == str)
	{
		return _T("Перед удалением группы, пожалуйста, удалите все задания и расписания из неё");
	}
	if (context == 0 && CString(_T("Schedule")) == str)
	{
		return _T("Расписание");
	}
	if (context == 0 && CString(_T("Description")) == str)
	{
		return _T("Описание");
	}
	if (context == 0 && CString(_T("Can't create Task file: %s")) == str)
	{
		return _T("Невозможно создать задание: %s");
	}
	if (context == 0 && CString(_T("Not set")) == str)
	{
		return _T("Не установлено");
	}
	if (context == 0 && CString(_T("About %s")) == str)
	{
		return _T("О %s");
	}
	if (context == 0 && CString(_T("User \"%s\" already exist. Please change the name.")) == str)
	{
		return _T("Пользователь \"%s\" уже существует. Пожалуйста, поменяйте имя.");
	}
	if (context == 0 && CString(_T("User doesn't have Client right")) == str)
	{
		return _T("У пользователя нет прав клиента");
	}
	if (context == 0 && CString(_T("Can't start vnc server")) == str)
	{
		return _T("Невозможно запустить сервер vnc");
	}
	if (context == 0 && CString(_T(" Schedule files (*.sch)|*.sch|All Files (*.*)|*.*||")) == str)
	{
		return _T(" Файлы расписания (*.sch)|*.sch|Все Файлы (*.*)|*.*||");
	}
	if (context == 0 && CString(_T("SuperSchedule contain Task(s) witn undefined View(s).\n\nDelete such tasks to fix the problem?")) == str)
	{
		return _T("Суперрасписание содержит задание(я) с неопределёнными окном(ами) просмотра.\n\nУдалить эти задания, чтобы исправить проблем?");
	}
	if (context == 0 && CString(_T("%s client on %s closed!")) == str)
	{
		return _T("%s клиент на %s закрыт!");
	}
	if (context == 0 && CString(_T("BitBlt 2 error")) == str)
	{
		return _T("ошибка BitBlt 2");
	}
	if (context == 0 && CString(_T("Low Flash quality")) == str)
	{
		return _T("Флеш низкого качества");
	}
	if (context == 0 && CString(_T("Schedule %04d")) == str)
	{
		return _T("Расписание %04d");
	}
	if (context == 0 && CString(_T("Shutdown computer")) == str)
	{
		return _T("Выключение компьютера");
	}
	if (context == 0 && CString(_T("\nClient's licenses: %d\tUsed: %d\n")) == str)
	{
		return _T("\nЛицензии клиента: %d\tИспользованы: %d\n");
	}
	if (context == 0 && CString(_T("Medium Flash quality")) == str)
	{
		return _T("Флеш среднего качества");
	}
	if (context == 0 && CString(_T("Unable to find logo picture file!")) == str)
	{
		return _T("Невозможно найти файл логотипа!");
	}
	if (context == 0 && CString(_T("%s Gb")) == str)
	{
		return _T("%s Гб");
	}
	if (context == 0 && CString(_T("Command")) == str)
	{
		return _T("Команда");
	}
	if (context == 0 && CString(_T(" second")) == str)
	{
		return _T(" секунда");
	}
	if (context == 0 && CString(_T("Move task to Group")) == str)
	{
		return _T("Переместить задание в Группу");
	}
	if (context == 0 && CString(_T("Full Name")) == str)
	{
		return _T("Полное Имя:");
	}
	if (context == 0 && CString(_T("Run periodically")) == str)
	{
		return _T("Запуск периодически");
	}
	if (context == 0 && CString(_T("%d seconds before close")) == str)
	{
		return _T("%d секунд до закрытия");
	}
	if (context == 0 && CString(_T("Task name")) == str)
	{
		return _T("Имя задания");
	}
	if (context == 0 && CString(_T("Do you want to restart schedule on station:\n")) == str)
	{
		return _T("Вы хотите перезапустить расписание на станции:\n");
	}
	if (context == 0 && CString(_T(" day, ")) == str)
	{
		return _T(" день, ");
	}
	if (context == 0 && CString(_T("Do you want to delete schedule?")) == str)
	{
		return _T("Вы действительно хотите удалить расписание?");
	}
	if (context == 0 && CString(_T("Message")) == str)
	{
		return _T("Сообщение");
	}
	if (context == 0 && CString(_T("Server: %s")) == str)
	{
		return _T("Сервер: %s");
	}
	if (context == 0 && CString(_T("Error connect to server: %s")) == str)
	{
		return _T("Ошибка подключения к серверу: %s");
	}
	if (context == 0 && CString(_T("Task Mngr (%s)")) == str)
	{
		return _T("Мнджр заданий (%s)");
	}
	if (context == 0 && CString(_T("Image")) == str)
	{
		return _T("Изображение");
	}
	if (context == 0 && CString(_T("%.2f seconds / pixel (%.2f pixels / second)")) == str)
	{
		return _T("%.2f секунд / пиксель (%.2f пикселей / секунду)");
	}
	if (context == 0 && CString(_T("Not known MAC address of %s player box.")) == str)
	{
		return _T("Неизвестный MAC-адрес %s плеера.");
	}
	if (context == 0 && CString(_T("You should fill not null path to media!\n")) == str)
	{
		return _T("Вы должны ввести ненулевой путь к файлам!\n");
	}
	if (context == 0 && CString(_T("RSS")) == str)
	{
		return _T("RSS");
	}
	if (context == 0 && CString(_T("Text files (*.txt)|*.txt|All Files (*.*)|*.*||")) == str)
	{
		return _T("Текстовые файлы (*.txt)|*.txt|Все файлы (*.*)|*.*||");
	}
	if (context == 0 && CString(_T("PowerPoint")) == str)
	{
		return _T("PowerPoint");
	}

	assert(0 && "Untranslated string detected!");
	return str;
#else
	return str;
#endif
}
